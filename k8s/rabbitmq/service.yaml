apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
spec:
  selector:
    app: rabbitmq
  ports:
    - name: queue
      protocol: TCP
      port: 5672
      targetPort: 5672
    - name: ui
      protocol: TCP
      port: 15672
      targetPort: 15672
  type: ClusterIP