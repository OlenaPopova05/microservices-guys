apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
spec:
  replicas: 1
  selector: 
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: notification-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080

          env:
            - name: ASPNETCORE_URLS
              valueFrom:
                configMapKeyRef:
                  name: notification-service-config
                  key: ASPNETCORE_URLS
            - name: ConnectionStrings__NotificationsDb
              valueFrom:
                secretKeyRef:
                  name: notification-service-secret
                  key: ConnectionStrings__NotificationsDb
            - name: RabbitMQ__Host
              value: rabbitmq
            - name: RabbitMQ__Username
              value: guest
            - name: RabbitMQ__Password
              value: guest
                  
          startupProbe:
            httpGet:
              path: /health
              port: 8080
            failureThreshold: 30
            periodSeconds: 5

          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            periodSeconds: 10

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"